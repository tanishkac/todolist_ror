<h1>Edit Task</h1>

<%= form_with model: @task do |form| %>
  <% if @task.errors.any? %>
    <ul>
      <% @task.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  <% end %>

  <%= form.label :title %>
  <%= form.text_field :title %>
  <%= form.submit %>
<% end %>